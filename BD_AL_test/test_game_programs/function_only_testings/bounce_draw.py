def draw(x, y):
    """Move ball and draw game."""
    # comenting lines below to emulate with a custom input and
    # avoiding using object and class constructors.
    # ball.move(aim)

    #x = ball.x
    #y = ball.y

    if x < -200 or x > 200:
        pass
        #aim.x = -aim.x

    if y < -200 or y > 200:
        pass
        #aim.y = -aim.y

    #clear()
    #goto(x, y)
    #dot(10)

    #ontimer(draw, 50)